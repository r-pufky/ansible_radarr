---
###############################################################################
# Restore
###############################################################################
# Find the latest set of backups and copy to remote. Restart services and
# validate they are running from role.
#
# Use synchronize instead of unarchive as metadata may be multi-GB.

- name: 'Restore | filesystem | enumerate all backups'
  delegate_to: 'localhost'
  ansible.builtin.find:
    paths: '{{ radarr__cfg._backup_d }}'
    patterns: '*.tar.xz'
    file_type: 'file'
    recurse: false
  register: radarr__archive

# Grab datestamp from expected 'YYYY-MM-DDTHHMMSS_{FILE}' format.
- name: 'Restore | fact | parse latest backup'
  when: radarr__archive.files | length > 0
  ansible.builtin.set_fact:
    radarr__restore_archive: '{{

        (
          radarr__archive.files |
          sort(attribute="mtime", reverse=true) |
          first
        ).path
      }}'

- name: '{{ "Restore | copy | " ~ radarr__restore_archive }}'
  ansible.posix.synchronize:
    src: '{{ radarr__restore_archive }}'
    dest: '{{ "/tmp/" ~ radarr__restore_archive | basename }}'

- name:
    '{{ "Restore | filesystem | /tmp/" ~ radarr__restore_archive | basename }}'
  ansible.builtin.command: '{{
      "/usr/bin/tar -xJpf " ~
      "/tmp/" ~ radarr__restore_archive | basename ~
      " -C " ~ radarr___app._var_d ~ " --overwrite"
    }}'
  changed_when: true

- name: 'Restore | filesystem | cleanup'
  ansible.builtin.file:
    path: '{{ "/tmp/" ~ radarr__restore_archive | basename }}'
    state: 'absent'

- name: 'Restore | start'
  ansible.builtin.import_tasks: 'start.yml'

- name: 'Restore | success ✔'
  ansible.builtin.meta: 'end_host'
